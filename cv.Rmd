---
title: "cv"
output: html_document
---

```{R}
# load libraries
library(tidyverse)
library(plotly)

# Graph the cars vector with all defaults
carplot <- ggplot(data = mtcars, aes(x = mpg, y = cyl)) +
  geom_point()
carplot
```